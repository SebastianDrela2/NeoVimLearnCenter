#include <iostream>
#include <string>

int main()
{
    std::string text;
    std::getline(std::cin, text);

    int textLength = text.length();

    std::cout << "Total text length: " << textLength << std::endl;

    if (true)
    {
        
    }
}
